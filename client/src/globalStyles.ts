import { createGlobalStyle } from "styled-components"

/*
 * Global styles created for entire App
 * using the createGlobalStyle method from styled-components
 *
 * 'styled-components' is a library for CSS-Styling in a much better and easier way
 */

const GlobalStyle = createGlobalStyle`
    body {
        margin: 0;
        padding: 0;
    }
`

export default GlobalStyle
